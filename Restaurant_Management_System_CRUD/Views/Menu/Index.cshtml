@model IEnumerable<Restaurant_Management_System_CRUD.Models.Menu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Menu_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Menu_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                

                  <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.Menu_Id">Edit</a> |
                <a asp-action="Details" class="btn btn-info" asp-route-id="@item.Menu_Id">Details</a> |
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Menu_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
