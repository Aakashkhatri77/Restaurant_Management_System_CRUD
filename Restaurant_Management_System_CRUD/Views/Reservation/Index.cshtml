@model IEnumerable<Restaurant_Management_System_CRUD.Models.Reservation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@*<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>*@
@*<div class="aa-search-box">
    <form asp-action="Index" method="get">
        <input type="text" name="searchItems" placeholder="Search">
        <button type="submit" class="btn btn-success my-2 my-sm-0"><span>Search</span></button>
    </form>
</div>*@
<table class="table table-bordered table-sm">
    <thead style="background-color:black; text-align:center; color:white; font-size:small;">
        <tr>
            <th>CheckIn</th>
            <th>Table Types</th>
            <th>No. of Chair </th>
            <th>No. of Persons</th>
@*            <th>Table Image</th>
*@        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="table-primary">
                <td>
                    @Html.DisplayFor(modelItem => item.CheckIn)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Table.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Table.NoOfChair)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NoOfPersons)
                </td>
              @*  <td>
                    @Html.DisplayFor(modelItem => item.Table.Image)
                </td>*@
                <td>
                    <a asp-action="Create" asp-route-id="@item.Id" class="btn btn-outline-success">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-warning">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

